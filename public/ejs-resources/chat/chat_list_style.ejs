<style>
    #chat-list {
        display: flex;
        flex-direction: row;
        align-items: flex-start;
        justify-content: flex-start;
        width: 100%;
        height: 100%;
        max-width: 400px;
        min-width: 270px;
        position: relative;
        transition: max-width 0.5s ease, min-width 0.5s ease;
    }

    #shelter-list-container {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        width: 100%;
        height: 100%;
        border-right: 1px solid #e0e0e0;
        overflow-x: hidden;
    }

    #chat-list.toggled {
        max-width: 30px;
        min-width: 30px;
        overflow-x: hidden;
        transition: max-width 0.5s ease, min-width 0.5s ease;
    }

    @keyframes chat_list {
        0% {
            overflow-x: auto;
        }
        100% {
            overflow-x: hidden;
        }
    }

    #chat-list-toggle-btn-container {
        position: absolute;
        top: 50%;
        right: -26px;
        transform: translateY(calc(-50% + 60px));
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
        max-width: 25px;
        max-height: 60px;
        background-color: #fff;
        cursor: pointer;
        border-top-right-radius: 10px;
        border-bottom-right-radius: 10px;
        z-index: 1;
        transition: background-color 0.5s ease;
        transition: max-width 0.5s ease;
        border-right: 1px solid #e0e0e0;
    }

    #chat-list-toggle-btn-container > svg {
        transform: rotate(90deg);
    }

    #chat-list-toggle-btn-container.toggled {
        position: absolute;
        bottom: 0;
        left: 0;
        background-color: #fff;
        cursor: pointer;
        transition: background-color 0.5s ease;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        max-width: 29px;
        max-height: 100%;
        animation: move_btn 1s ease;
        animation-delay: 0s;
        animation-fill-mode: forwards;
        border-right: 1px solid #e0e0e0;
    }

    @keyframes move_btn {
        0% {
            opacity: 0;
        }
        100% {
            opacity: 1;
        }
    }

    #chat-list-toggle-btn-container.toggled > svg {
        transform: rotate(-90deg);
        transition: transform 0.5s ease;
    }

    #chat-list-title {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: flex-start;
        width: 100%;
        height: 60px;
        background-color: #fff;
        border-bottom: 1px solid #e0e0e0;
    }

    #chat-list-title h1 {
        font-size: 1.5rem;
        font-weight: 500;
        margin: 0;
        padding: 0 20px;
    }

    #no-chats-text {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 60px;
        background-color: #fff;
    }

    #no-chats-text.invisible {
        display: none;
    }

    #no-chats-text p {
        font-size: 1rem;
        font-weight: 400;
        margin: 0;
        padding: 0 20px;
        color: #9e9e9e;
    }
    :root {
        --extended-height: 120px;
    }
    #shelter-list-loading {
        width: 100%;
        height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: fadein 1s ease;
        max-height: 200px;
    }

    @keyframes fadein {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    #shelter-list-loading.invisible {
        display: flex;
        max-height: 0;
        animation: fadeout 0.5s ease;
    }

    @keyframes fadeout {
        from {
            opacity: 1;
            max-height: 200px;
        }
        to {
            opacity: 0;
            max-height: 0;
        }
    }
    .shelter-list-item-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        width: 100%;
        min-height: 60px;
        height: fit-content;
        background-color: #fff;
        border-bottom: 1px solid #e0e0e0;
        cursor: pointer;
        max-height: 60px;
        transition: max-height 0.5s ease;
        overflow: hidden;
    }

    .shelter-list-item-image {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        margin: 10px;
        border: 1px solid #079292;
    }

    .shelter-list-item-name {
        font-size: 1rem;
        font-weight: 500;
        margin: 0;
        padding: 0;
        flex: 1;
        align-items: flex-start;
        width: 100%;
        display: inline-block;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .shelter-list-item-info-container {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        height: 60px;
        width: 100%;
        transition: background-color 0.5s ease;
        background-color: #fff;
    }

    .shelter-list-item-info-container:hover {
        background-color: #f5f5f5;
        transition: background-color 0.5s ease;
    }

    .shelter-list-item-toggle-button-container {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        height: 100%;
        width: 60px;
        transform: rotate(-90deg);
        transition: transform 0.5s ease;
    }

    .shelter-list-item-toggle-button-container.toggled {
        transform: rotate(0deg);
        transition: transform 0.5s ease;
    }

    .shelter-list-item-chats-container {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        width: 100%;
        height: fit-content;
        overflow: hidden;
        padding-left: 20px;
        box-sizing: border-box;
    }

    .shelter-list-item-chat-container {
        display: flex;
        flex-direction: row;
        align-items: flex-start;
        justify-content: flex-start;
        width: 100%;
        height: 60px;
        transition: background-color 0.5s ease;
    }

    .shelter-list-item-chat-container:hover {
        background-color: #f5f5f5;
        transition: background-color 0.5s ease;
    }

    .shelter-list-item-chat-image {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        margin: 10px;
        border: 1px solid #079292;
    }

    .shelter-list-item-chat-info-container {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: center;
        height: 100%;
        width: calc(100% - 70px);
    }
    
    .shelter-list-item-chat-name {
        font-size: 1rem;
        font-weight: 500;
        margin: 0;
        padding: 0;
        width: 100%;
        display: inline-block;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .shelter-list-item-chat-last-message {
        font-size: 0.8rem;
        font-weight: 400;
        margin: 0;
        padding: 0;
        color: #9e9e9e;
        width: 100%;
        display: inline-block;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    @media screen and (max-width: 600px) {
        #chat-list {
            max-width: 100%;
            min-width: 100%;
            border-right: none;
        }

        #shelter-list-container {
            width: calc(100% - 30px);
        }

        #chat-list-toggle-btn-container {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
            max-width: 30px;
            max-height: none;
            background-color: #f5f5f5;
            cursor: pointer;
            top: auto;
            left: auto;
            bottom: auto;
            right: auto;
            transform: none;
            transition: max-width 0.5s ease;
        }

        #chat-list-toggle-btn-container > svg {
            transform: rotate(90deg);
        }

        #chat-list-toggle-btn-container.toggled {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 29px;
            height: 100%;
            max-width: 30px;
            max-height: none;
            background-color: #fff;
            cursor: pointer;
            top: auto;
            left: auto;
            bottom: auto;
            right: auto;
            transform: none;
            transition: max-width 0.5s ease;
            border-right: 1px solid #e0e0e0;
        }

        @keyframes move_btn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }

        #chat-list-toggle-btn-container.toggled > svg {
            transform: rotate(-90deg);
            transition: transform 0.5s ease;
        }
    }
</style>