<style>
    #search-bar {
        width: 100%;
        height: 60px;
        background-color: #079292;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 10px 8px;
        box-sizing: border-box;
        z-index: 1000;
        position: sticky;
        top: 0;
        left: 0;
        font-family: 'Lato', sans-serif !important;
        min-width: 100%;
    }

    #search-input,
    #search-input-container,
    #search-form {
        height: 100%;
        flex: 1;
        max-width: 600px;
        border-width: 0;
        padding-left: 10px;
        padding-right: 10px;
        border-radius: 2px;
        box-sizing: border-box;
        box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px;
        position: relative;
    }

    #search-input-container > hr {
        height: calc(100% - 10px);
        width: 1px;
        background-color: #d3d3d3;
        margin: 0 10px;
        color: #d3d3d3;
        border-width: 0;
    }

    #search-form {
        padding: 0;
    }

    #search-input {
        box-shadow: none;
	    white-space: nowrap;
	    text-overflow: ellipsis;
    }

    #search-input:focus {
        outline: none;
    }

    #search-input::placeholder {
        color: #d3d3d3;
        user-select: none;
        font-family: 'Roboto', sans-serif;
    }

    #menu-button,
    .mobile {
        display: none;
    }

    .desktop-elements {
        display: flex;
    }

    .nav-buttons {
        user-select: none;
        cursor: pointer;
        color: white;
        font-size: 12pt;
        transform: scale(1);
        transform-origin: center;
        transition: 0.15s;
        padding: 0 10px;
        box-sizing: border-box;
        font-family: 'Lato', sans-serif;
    }

    .nav-buttons:hover {
        transition: 0.15s;
        transform: scale(1.025);
    }


    #profile-pic {
        width: 40px;
        height: 40px;
    }

    #search-input-container {
        min-width: 200px;
    }

    .top-bar-section-cont {
        width: calc(100% / 3);
        height: 100%;
        display: flex;
        flex: none;
    }
    #notifications-icon-container {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        height: 100%;
        margin: 0 15px;
        visibility: visible;
    }

    #notifications-icon {
        width: 24px;
        height: 24px;
        fill: white;
        cursor: pointer;
    }
    #toggle-user-menu-btn {
        margin-right: 5px;
        cursor: pointer;
    }

    #user-menu-image-container {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        cursor: pointer;
    }

    .search-bar-display-name {
        width: 75px;
        min-width: 50px;
        text-overflow: ellipsis;
        padding: 0 8px;
        overflow: hidden;
        white-space: nowrap;
        color: white;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
    }

    #logo-image-cont {
        flex-direction: row;
        align-items: center;
    }

    #filters-btn {
        color: #d3d3d3;
        cursor: pointer;
        user-select: none;
        position: relative;
        transition: 0.15s;
    }

    #filters-btn.in-use::before {
        background-color: #079292;
        content: '';
        position: absolute;
        top: 0;
        right: 0;
        width: 8px;
        height: 8px;
        border-radius: 50%;
    }

    #filters-btn.selecting {
        color: #079292;
        transition: 0.15s;
    }

    #filters-btn:hover {
        color: #079292;
        transition: 0.15s;
    }

    #filters-container {
        width: 100%;
        height: 100vh;
        max-height: 300px;
        background-color: white;
        position: absolute;
        top: calc(100% + 5px);
        left: 0;
        z-index: 1000;
        border-radius: 2px;
        box-shadow: rgba(0, 0, 0, 0.05) 1.95px 1.95px 2.6px;
        overflow-y: scroll;
        overflow-x: hidden;
        transition: max-height 0.3s ease-in-out;
    }

    #filters-container.hidden {
        max-height: 0;
        overflow-y: hidden;
        transition: max-height 0.3s ease-in-out;
    }

    @media screen and (max-width: 1184px) {

        #search-input,
        #search-input-container,
        #search-form {
            max-width: 400px;
        }

        #search-form {
            padding: 0;
        }

        #search-input-container {
            padding: 0 10px;
            box-sizing: border-box;
        }

        #menu-button,
        .mobile {
            display: flex;
        }

        .desktop-elements {
            display: none !important;
        }

        .top-bar-section-cont {
            flex: 1;
            justify-content: center;
        }


        #userSpace {
            flex: none !important;
            width: fit-content;
            margin-left: 10px;
            display: flex;
            flex-direction: row-reverse;
            justify-content: center !important;
            align-items: center;
        }

        #logo-image-cont {
            max-width: 40px;
            margin-right: 10px;
        }
    }

    @media screen and (max-width: 645px) {
        .top-bar-section-cont {
            flex: 1;
            justify-content: center;
        }

        #notifications-icon-container {
            display: none;
        }

        #userSpace {
            flex: none !important;
            width: 40px;
            margin-left: 10px;
            display: flex;
            flex-direction: column;
            justify-content: center !important;
            align-items: center;
        }

        #logo-image-cont {
            max-width: 40px;
            margin-right: 10px;
        }
    }

    @media screen and (max-width: 480px) {

        #search-input,
        #search-input-container,
        #search-form {
            height: 100%;
            width: 100%;
            border-width: 0;
            padding-left: 10px;
            padding-right: 10px;
            border-radius: 50px;
        }

        #search-form {
            padding: 0;
        }

        #search-input-container {
            padding: 0 10px;
            box-sizing: border-box;
        }

        #profile-pic {
            width: 30px;
            height: 30px;
        }

        #userSpace {
            flex: none !important;
            width: 40px;
            margin-left: 10px;
            display: flex;
            flex-direction: column;
            justify-content: center !important;
            align-items: center;
        }

        #logo-image-cont {
            max-width: 40px;
            margin-right: 10px;
        }

        #filters-container {
            width: 100%;
            height: 100vh;
            max-height: 600px;
            background-color: white;
            position: absolute;
            top: calc(100% + 5px);
            left: 0;
            z-index: 1000;
            border-radius: 2px;
            box-shadow: rgba(0, 0, 0, 0.05) 1.95px 1.95px 2.6px;
            overflow-y: scroll;
            overflow-x: hidden;
            transition: max-height 0.3s ease-in-out;
        }
    }
</style>